# Effect migration ledger

This ledger is the per-slice record required by the Effect migration plan. Update the relevant row
before starting the next work package. A compatibility bridge must name its deletion package; an
unstable import or detached task must name its owning adapter or registry entry; and the rollback
column must contain the local commit that cleanly removes the slice.

The baseline starts from branch `fd/v1-effect-feasibility` at
`a9fb3a2abf207c5805d4d9f0d9661fb124a4a49f`. The pre-existing dirty documentation scope and the
machine/runtime facts are preserved in [baseline.md](baseline.md) and its JSON companions.

| Work package | Status | Compatibility bridge | Unstable imports | Detached work allowlist | Focused tests | Benchmark delta | Rollback commit |
|---|---|---|---|---|---|---|---|
| P0 — evidence | Complete | None | None | None | 1,223 source pass + 1 skip; 1,214 shipped pass + 10 skip; exact-cohort probe; four paired comparisons; CI policy 3/3 | Floor recorded: bundle 566,619 B raw / 147,431 B gzip; pack 453,844 B; legacy exec p50 10.254 ms; all stable evidence equal | `aeda84b` |
| P1 — explicit resource handles | Complete ([evidence](p1.md)) | Plain `DaemonResources` owner, replaced by `LifecycleCoordinator` in P4; legacy subprocess joins move to P3/P5 | None | Existing `execFileP`/agents poll and `FLEETDECK_SPAWN_CMD` child interruption only, explicitly closed by P3/P5; all late store callbacks are phase-gated and joined | 123 focused pass; 1,260 source pass + 1 skip; 1,251 shipped pass + 10 skip; real SIGTERM, held-hook, listener/WS/terminal/child, prefix/double-close, timer and DB-order proofs | Bundle +31,683 B raw / +6,321 B gzip; pack +3,561 B; unchanged exec p50 -16.862%, p95 -0.786%, p99 +0.140%; comparator healthy | `a8e4c9b` |
| P2 — exact Effect kernel | Complete ([evidence](p2.md)) | In-memory/fake service Layers and generic Bun-platform conformance helper; P3 supplies the live process adapter | None; the exact unstable-import register is empty | None | 31 focused pass; 1,284 source pass + 1 skip; 1,275 shipped pass + 10 skip; exact-version, import-boundary, source/generated scan, natural-exit and keep-alive fixtures | 20-package closure / 53,823,214 B installed; kernel 97,531 B raw / 23,058 B gzip, +14,075 B (+16.865%) versus P0 standalone probe; no production behavior path changed | `46a5c02` |
| P3 — Bun subprocess pilot | Complete ([evidence](p3.md)); implementation and correctness gates complete. Paired P0→P3 performance evidence closed by adaptation: unmeasurable as specified at HEAD (P4 deleted the measured bootstrap subject; the macOS checkpoint host is unavailable and the machine-aware comparator rejects cross-host compares). Daemon/HTTP half superseded by P4/P6.8; exec half re-run at HEAD/WSL2 as absolute evidence (comparator ok:true) | One `BootstrapRuntimeBridge` for Promise callers, deleted atomically in P4 | `effect/unstable/process/*` only in the comparison Bun adapter, with exact rows in the unstable-import register | Process-group detachment only through the process-driver registry | 67 focused pass; 1,333 source pass + 1 skip; 1,324 shipped pass + 10 skip; 7 packed-install/signal cases; exact differential exec/files/cancellation/tree/reap/cap/UTF-8/cwd/stdin/env coverage | Static bundle 694,295 B raw / 175,309 B gzip within budget (committed at `bcf3337`). Paired P0→P3 timing unmeasurable at HEAD (host + subject changed); daemon/HTTP half superseded by P6.8 within-host quiet-WSL2 (12/12 `/health`+`/state` p95 within +10%) and P4 shutdown evidence; exec half re-run at HEAD/WSL2 as absolute — comparator ok:true, 13/13 verification, zero residue, stable-evidence SHA `fd539eac…fdc0` equal — not a P0 delta | `bcf3337` implementation anchor; no separate final-evidence commit (evidence closed by adaptation/supersession, no new committed JSON) |
| P4 — root runtime and shutdown | Checkpointed at `661dfe3` ([evidence](p4.md)); final isolated source/bundle global counts pending two recorded source failures | P3 `BootstrapRuntimeBridge` deleted; temporary `execFileP` facade now submits only through the one root `IngressSupervisor` and is deleted in P13 | No new unstable imports; `BunRuntime.runMain`/custom teardown stay at the root Bun boundary and P3 comparison-only rows are unchanged | None added; registered process groups only, while bounded acquisition retains an observed late-retirement continuation rather than abandoning it | 124 focused pass; ten-checkpoint failure/interruption matrix; first/repeated signals in every phase; active held HTTP/WS/process/mDNS; exact exit/log/readiness/takeover; generated boundary/policy/freshness 22/22; packed installed-bin signal gate green | Source idle p95 6.676 ms / bundle 6.014 ms; worst forced 223.079 / 220.594 ms under 1,750 ms; separate mDNS p95 219.746 / 219.634 ms under 1,000 ms; zero residue in 100 measured + 10 warmup runs; bundle 688,199 B raw / 186,978 B gzip within budget | `661dfe3` implementation checkpoint; parent `bcf3337` is the full P3 rollback target |
| P5 — boot and schedules | Complete ([evidence](p5.md)) at `ca62b94f` | Legacy agents-poll and network-watch implementations remain in-tree as compatibility surface, not live cadence owners; `createCore` no longer owns retention; question orphan sweep stays on the P1 handle until P10; aggregate Background owner is production-registered before lifecycle sealing | None added; P3 comparison-only `effect/unstable/process/*` rows unchanged | One reviewed deadline-bounded exception: the single `Effect.forkDetach` in `src/daemon/app/background-owner.ts`, so a stuck legacy Promise cannot make root Scope closure exceed the P4 hard deadline. The owner is manually registered, interrupted, and bounded-joined. Making every legacy bridge cancellation-bounded now was rejected; those bridges are slated for later packages. See [p5.md](p5.md). | Quiet WSL2 HEAD `ca62b94f`: `bun run test` 1,493 pass / 0 fail / 186 files / 538.60 s; `bun run test:bundle` 1,484 pass / 9 skip / 0 fail. Bundle policy 2/2. Prompt-failure regression `tests/effect/background-prompt-failure.test.ts` | Daemon 651,685 B raw (ceiling 768,000) / 188,452 B gzip-9 zlib (ceiling 189,440, 988 B under); SHA-256 `89b3ef7723961102cb55f986f1710524ee96d55a093db09e7b963c324ee801d9`; double-build byte-identical. Versus P4: raw -36,514 B, gzip +1,474 B, still under ceiling. No ceiling change, no whitespace minification | `ca62b94f` implementation checkpoint. Whole-slice only: revert `972621d5` through `ca62b94f` to return to P4 root-cutover `661dfe31`. No per-scheduler rollback remains |
| P6 — HTTP and WebSocket workflows | Complete ([p6-http-matrix.md](p6-http-matrix.md), [p6-route-wave.md](p6-route-wave.md), [p6-graceful-stop-verification.md](p6-graceful-stop-verification.md), [p6-bench-comparison.md](p6-bench-comparison.md)) at `b2d11d84` | Direct `http.bind` production entrypoint retired for `httpServer.bind`; `createHttp` transport retained. Sole callback bridge is `IngressSupervisor.runPromiseExit` via `HttpServer.runRequest`. Per-group `effectRoutes` facades (`installEffectRoutes`) remain until P13; `effectRoutes=null` is the per-group rollback. GET `/mail` and GET `/api/watch` stay under their P1 owners until P10; `/ws/term` stays behind the termbridge facade until P7; static assets stay legacy until P13 | No production `effect/unstable/http/*` imports. P6.7 KEEP CUSTOM ADAPTER ([p6-transport-trial.md](p6-transport-trial.md)); P3 comparison-only `effect/unstable/process/*` rows unchanged | One reviewed, bounded, test-pinned exception-of-record: the unref'd `HOOK_REPLY_FLOOR_MS` reply-floor timer in `settleEffectHookRoute` (`src/daemon/http.ts`), so a wedged Effect runtime still emits the canonical hook `200 {}` under `idleTimeout:0`. Idempotent, emit-first/commit-after, never truncates a HOLD (holds never reach the settler). Pinned by `tests/effect/http-workflow-hooks.test.ts`. Recorded alongside the P5 `Effect.forkDetach` exception in `background-owner.ts`; no other detached work added | Quiet WSL2 HEAD `b2d11d84`: `bun run test` 1,578 pass / 6 skip / 0 fail; `bun run test:bundle` rerun 1,569 pass / 15 skip / 0 fail. The 6/15 skips are the fail-open contract's declared fault-injection skips plus platform skips. One suite-context flake occurrence in `tests/process-driver-reference.test.ts` ("Node reference close is idempotent...", 1030 ms) on a bundle-mode full run; 5/5 green in isolation. Freeze + contract + workflow + owner + graceful-stop cited suites green | Daemon 603,458 B raw / 164,890 B gzip-9 zlib (ceiling 189,440, 24,550 B under) / SHA-256 `cab4fbc2f66565d72e52f62c52e00edd73c359798fc5f590f18fe43cebb7c874`; deterministic. P6.8: 12/12 `/health`+`/state` p95 cells PASS the +10% line ([p6-bench-comparison.md](p6-bench-comparison.md), [p6-postconv.json](p6-postconv.json) vs [p6-baseline.json](p6-baseline.json)). `command` is a tenth harness workload (`POST {text: note}` → 200 `core.command`); quiet-host numbers pending the next idle-machine slot | Per-group: `effectRoutes=null` (unset `installEffectRoutes` in `program.ts`). Whole-slice: revert `307fae0a` through `b2d11d84` (P6.1 freeze `307fae0a`/`46e13c50`/`9332d576`, P6.2 `d425cc96`/`3735759e`, P6.3 `e7900bac`/`51d39ddd`, plus the route wave `56a15e8a`..`b2d11d84`) restores the P5 completion tree at `67758ba9` / `ca62b94f` |
| P7 — terminal ownership/transport | Not started | Legacy stream decoder allowed only through the measured transport decision | Terminal adapter registry only | None permitted | Byte-order/parser/bridge, viewer lifecycle, backpressure, real-tmux macOS/Linux soak | Pending | Pending |
| P8 — store/SQLite workflows | Complete ([p8-strict-trial.md](p8-strict-trial.md), [p8-stmt-cache-trial.md](p8-stmt-cache-trial.md), [p8-sql-client-trial.md](p8-sql-client-trial.md), [p9-completion-map.md](p9-completion-map.md)) at `cd0470bb` | Per-slice `STORE_BACKED_*` flags with legacy adapters in-tree; HTTP-bridged keep capability params (P6.4). P8.3 Store owner is the live lifetime seam | SQLite adapter registry only; P8.7 trial did not import `effect/unstable/sql/*` (archival `fd/p8-sqltrial` @ `a673431e`) | None added | Quiet WSL2 HEAD `cd0470bb`: `bun run test` 1,658 pass / 6 skip / 0 fail; `bun run test:bundle` 1,649 pass / 15 skip / 0 fail. Focused: sqlite characterization, strict trial, store-owner close-order, stmt-cache, db-workflows family (retention/boot/ingest/lan-tick/spawn-liveness) | Daemon 607,404 B raw / 165,586 B gzip-9 zlib (ceiling 189,440, 23,854 B under) / SHA-256 `1f601ddb497f1129da62bbc601ef8af83a1c6b3f3ba339a3e48b979e9ebcde66`; deterministic | Per-slice: flip `STORE_BACKED_BOOT` / `STORE_BACKED_RETENTION` / `STORE_BACKED_AGENTS_INGEST` / `STORE_BACKED_LAN_TICK` / `STORE_BACKED_LIVENESS` to `false`. Whole-slice: revert `05b40bd5`, `346ee85a`, `4ff3e393`, `351b376f`, `145e9fbd`, `50412bd8`, `917c4dc8`, `e0ab862a`, `570d8dae`, `cd0470bb` restores `37e07659`. Do not range-revert `05b40bd5^..cd0470bb` (drops interleaved P6.8 `742168a4`) |
| P9 — application workflows | Not started | Caller façades deleted as each workflow completes | App workflow adapters only | Only plan-approved process-independent work | Domain parity plus cancellation/error/finalization suites | Pending | Pending |
| P10 — holds and fail-open | Not started | Hook wire façade retained; manual hold maps deleted on closure | None outside hold service adapter | None permitted | Hook/needs-you/board-hold failure and race matrix; canonical `200 {}` release-all | Pending | Pending |
| P11 — remaining Bun adapters | Not started | Only explicitly accepted KEEP/DEFER adapters | Capability registry with evidence link per use | Process detachment registry only | Per-capability conformance and soak suites | Pending | Pending |
| P12 — build and distribution | Not started | Selected builder is the only production pipeline | Builder adapter only if unstable | None permitted | Reproducible source/generated/packed-install parity and artifact scans | Pending | Pending |
| P13 — cleanup, observability, docs | Not started | No stale bridge allowed | Registry must match remaining imports exactly | Registry must match remaining detached work exactly | Boundary, observability, docs/plan consistency and full global gate | Pending | Pending |
| P14 — RC rehearsal and release gate | Not started | None added by the rehearsal | Updated exact-cohort registry | No new detached work | Exact cohort bump, full local and authorized remote matrix | Pending | Pending |
