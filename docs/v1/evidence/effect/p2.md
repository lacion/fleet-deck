# P2 exact Effect-kernel checkpoint

P2 completed on Bun `1.3.14`
(`0d9b296af33f2b851fcbf4df3e9ec89751734ba4`) at implementation and rollback
anchor `46a5c02`. The production daemon still starts through the imperative P1
entrypoint: this checkpoint pins the Effect RC, establishes the application
kernel and test infrastructure, and measures their cost without changing a
production behavior path.

## Kernel and policy established

- Exact `effect@4.0.0-rc.110` and `@effect/platform-bun@4.0.0-rc.110`
  dependencies, with `@effect/platform-node-shared@4.0.0-rc.110` locked in the
  same cohort and `@types/bun@1.3.14` aligned to the blocking runtime floor.
- V4-native service contracts use `Context.Service`, `Layer.succeed`, and
  `Layer.effect`. The Live Layer is definition-only; importing it acquires no
  resource and creates no runtime.
- Fake Layers plus `bun:test` helpers preserve typed failures, defects, and
  complete `Exit` values while exposing the RC.110 `TestClock` and
  `TestConsole` services.
- Source and generated-artifact tripwires keep contracts, board, and hook
  artifacts Effect-free; domain modules cannot import application/platform
  composition; native implementations can be selected only by the Live Layer.
- The unstable-import register is empty and exactly matches source. V3 package
  names and APIs are rejected.
- A generic acquire/case/release Bun-platform conformance harness is wired into
  the exact floor and advisory latest-stable types canary; the canary's no-save
  step hashes `package.json` and `bun.lock` to reject mutation.

No compatibility runtime bridge or detached Effect work exists at P2. The
`ProcessRunner` contract remains an injected fake until P3 supplies and proves
the direct `Bun.spawn` implementation.

## Dependency-closure audit

The audited install copied only `package.json` and `bun.lock` into a clean
scratch directory, then ran:

```text
bun install --production --frozen-lockfile --ignore-scripts
bun audit --json --audit-level=high
```

| Fact | P2 result | Gate |
|---|---:|---:|
| Direct production dependencies | 2 | 2 maximum |
| Resolved production closure | 20 packages | 24 maximum |
| Installed packages on Darwin arm64 | 15 | Informational |
| Installed `node_modules` | 53,823,214 B / 2,991 files | 75 MiB maximum |
| Clean-install harness elapsed | 147.190 ms | Recorded, same-machine only |
| Registry integrity records matched | 20 / 20 | Required |
| npm provenance present | 8 / 20 | All Effect cohort packages present |
| Signature or provenance present | 20 / 20 | Required |
| High/critical advisories | 0 | Required |

All installed licenses are MIT except `detect-libc@2.1.2` (Apache-2.0). The one
platform binary installed on the recorded machine is
`@msgpackr-extract/msgpackr-extract-darwin-arm64/node.napi.glibc.node`.
`msgpackr-extract@3.0.4` declares one reviewed `install` script; the clean audit
used `--ignore-scripts`, so it was not executed. `ws@8.21.2` and
`@types/ws@8.18.1` remained unchanged and deduped after
`platform-node-shared` entered the closure.

Two audited runs produced the same normalized stable-facts digest:
`f584ce11336ac424b51fe1caa9888faeb361910f76c0ebe463930ceb43d1021f`.
Every structural and measured hard gate passed.

## Representative kernel artifact

The kernel probe ran on Darwin `25.5.0`, Apple M4 Pro arm64, 14 logical CPUs,
and 24 GiB memory. Each evidence run executed the TypeScript fixture ten times,
built it twice with Bun for the Bun ESM target, and executed the bundled result
ten times. Source and bundle runs produced the same direct-argv result and
exited naturally.

| Artifact fact | Result |
|---|---:|
| Raw bundle | 97,531 B |
| Gzip (`Bun.gzipSync`, zlib level 9) | 23,058 B |
| Raw SHA-256 | `4caaa87b9a9618cf435f97e703c74de27d4350b1d979c7bd3b3f731461eaa897` |
| Gzip SHA-256 | `b6de5074893ff89f0a137e2ae16e17fd3d19466bc625312e55060e9145edea26` |
| P0 standalone Effect probe | 83,456 B raw |
| Raw delta from P0 probe | +14,075 B (+16.865%) |

Both runs were byte-for-byte deterministic and shared stable-evidence digest
`1b235a1c1c88b280b9edc797f599a224e211fbc2d5fb2c214af92c8fd20ff16e`.
Timing is recorded for diagnosis, not as a cross-machine comparison:

| Run | Build p50 / p95 | Source process p50 / p95 | Bundle process p50 / p95 |
|---|---:|---:|---:|
| 1 | 77.998 / 90.892 ms | 109.706 / 323.706 ms | 94.520 / 116.848 ms |
| 2 | 66.522 / 155.167 ms | 81.991 / 100.939 ms | 80.763 / 245.915 ms |

The probe comparison key was
`144d663f60a8e2e84bae68c41085990006af4d2e1fc0328a6690b570e062dd42`;
timings are comparable only when that key and machine load match.

## Verification

Completed checks on the implementation tree:

```text
bun install --frozen-lockfile --ignore-scripts
bun run effect:p2:check
bun run effect:p2:measure --out <cohort-report.json>
bun scripts/effect-migration/p2-kernel-probe.ts --out <kernel-run-1.json>
bun scripts/effect-migration/p2-kernel-probe.ts --out <kernel-run-2.json>
bun test --timeout 300000 <P2 focused matrix>  # 31 pass, 0 fail
bun run typecheck
bun run ci
```

The whole-tree run includes test-only cleanup-fixture isolation at `89a5a3c`:
the acceptance stub now leases an ephemeral port and reaps only its marker-verified
PID on assertion failure. This removed a concurrent fixed-port collision without
changing a shipped behavior path.

Final whole-tree closure was measured on the same committed implementation
tree, including one regenerated shipped-bundle run:

- Source suite: **1,284 pass, 1 skip, 0 fail** across 150 files.
- Shipped-bundle suite: **1,275 pass, 10 skip, 0 fail** across 150 files.

The focused matrix covers exact dependency/cohort policy, import boundaries,
generated board/hook scans, kernel Layers and typed errors, TestClock and
TestConsole, BunRuntime natural exit and root keepalive, the generic Bun
platform conformance contract, and blocking/latest CI policy.
