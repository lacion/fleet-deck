{
  "$schema": "./node_modules/@biomejs/biome/configuration_schema.json",
  // Single lint+format tool (replaces ESLint + Prettier + typescript-eslint).
  // Biome has no .gitignore coupling here — the include list below is the whole story.
  "vcs": { "enabled": false, "clientKind": "git", "useIgnoreFile": false },

  // ── File scope ──────────────────────────────────────────────────────────────
  // Faithful port of the old tooling's scope: eslint `lint` + prettier `format`
  // both targeted ONLY *.ts/*.tsx/*.mts/*.cts. Restricting the include set to
  // those four extensions automatically keeps every GENERATED .mjs/.js artifact
  // out of scope — src/daemon/fleetd.bundle.mjs, bin/fleetdeck.mjs,
  // scripts/fleet-*.mjs, board/vite.config.js, demo/*.mjs — with no per-file
  // ignore needed. The negations below drop vendored + built trees that DO carry
  // .ts/.tsx (node_modules types, the built board bundle under src/daemon).
  "files": {
    "includes": [
      "**/*.ts",
      "**/*.tsx",
      "**/*.mts",
      "**/*.cts",
      "!**/node_modules",
      "!src/daemon/board-dist",
      "!board/dist",
      "!dist"
    ]
  },

  // ── Formatter: mirrors .prettierrc.json exactly (commit B does the sweep) ─────
  // singleQuote:true semi:true tabWidth:2 useTabs:false printWidth:100
  // trailingComma:"all" arrowParens:"always". jsxSingleQuote was unset in Prettier
  // (→ double), matched by jsxQuoteStyle:"double".
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100,
    "lineEnding": "lf"
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "single",
      "jsxQuoteStyle": "double",
      "semicolons": "always",
      "trailingCommas": "all",
      "arrowParentheses": "always",
      "quoteProperties": "asNeeded",
      "bracketSpacing": true,
      "bracketSameLine": false
    }
  },

  // ── Linter ──────────────────────────────────────────────────────────────────
  // Sensible recommended baseline. Step 1 is a TOOL SWAP, not a lint-tightening:
  // every rule turned OFF below is a Biome-recommended rule that flags code the
  // old ESLint (js.recommended + typescript-eslint strict/stylistic, NO react /
  // a11y / react-hooks plugins) accepted. Disabling preserves parity without
  // touching source; re-tightening is a later, deliberate step.
  "linter": {
    "enabled": true,
    "rules": {
      "preset": "recommended",
      "a11y": {
        // No jsx-a11y plugin existed under ESLint — the whole a11y set is new.
        "noAutofocus": "off",
        "noStaticElementInteractions": "off",
        "noSvgWithoutTitle": "off",
        "useAriaPropsSupportedByRole": "off",
        "useKeyWithClickEvents": "off",
        "useSemanticElements": "off"
      },
      "complexity": {
        // Stylistic suggestions ESLint did not enforce.
        "noCommaOperator": "off", // sequence exprs in existing terse code
        // DIRECT CONFLICT with tsconfig noPropertyAccessFromIndexSignature (which
        // REQUIRES a['b'] for index signatures); the TS compiler setting wins.
        "useLiteralKeys": "off"
      },
      "correctness": {
        // React Hooks deps — no eslint-plugin-react-hooks was configured.
        "useExhaustiveDependencies": "off"
      },
      "security": {
        // dangerouslySetInnerHTML is used deliberately (rendered markdown/QR);
        // ESLint had no equivalent. Kept as a conscious trust-zone residual.
        "noDangerouslySetInnerHtml": "off"
      },
      "style": {
        // consistent-type-imports was OFF under typescript-eslint here.
        "useImportType": "off",
        // string-concat → template-literal rewrite (134 infos) — cosmetic.
        "useTemplate": "off"
      },
      "suspicious": {
        // Intentional ANSI/terminal control-char regexes (\x1b[...] etc.) — this
        // is a tmux/terminal tool; these regexes are the point.
        "noControlCharactersInRegex": "off",
        // Index-as-key in small static SVG/list renders ESLint never flagged.
        "noArrayIndexKey": "off",
        // Parenthesized assignments in loop/cond heads (ESLint no-cond-assign
        // defaulted to except-parens, which allowed these).
        "noAssignInExpressions": "off",
        // `let x;` (assigned later in try/catch) — TS/ESLint accepted these.
        "noImplicitAnyLet": "off",
        // Deliberate __proto__ access in a prototype-pollution defense test.
        "noProto": "off",
        // ${...} inside plain strings that are intentionally not templates.
        "noTemplateCurlyInString": "off",
        // forEach/map callbacks that intentionally return a value for side use.
        "useIterableCallbackReturn": "off"
      }
    }
  },

  // No import-organizing / assist actions — out of scope for a format-parity swap.
  "assist": { "enabled": false },

  // ── Boundary rules — graduated from tests/import-boundaries.test.ts (R2) ──────
  // Biome 2.5.8 noRestrictedImports `patterns.group` gitignore-style globs match
  // the RESOLVED import path (verified: a relative `../../src/daemon/db.ts` from
  // board/src matches `**/src/daemon/**` at any nesting depth), so the directional
  // bans are fully expressible here. The R2/P2 suite remains the stronger anchor
  // and also owns the hook seam allowlist, unstable-register join, v4 API
  // vocabulary, and generated-output fingerprints noRestrictedImports cannot
  // express.
  "overrides": [
    {
      "includes": ["board/src/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "patterns": [
                  {
                    "group": [
                      "**/src/daemon",
                      "**/src/daemon/**",
                      "effect",
                      "effect/**",
                      "@effect",
                      "@effect/**",
                      "kysely",
                      "kysely/**",
                      "hono",
                      "hono/**",
                      "bun:*"
                    ],
                    "message": "board/src talks to the daemon over HTTP, never by import, and must never pull effect/@effect or daemon-only dependencies into the browser bundle. See tests/import-boundaries.test.ts."
                  }
                ]
              }
            }
          }
        }
      }
    },
    {
      "includes": [
        "src/daemon/**",
        "!src/daemon/app/**",
        "!src/daemon/platform/**",
        "!src/daemon/fleetd.ts"
      ],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "patterns": [
                  {
                    "group": [
                      "**/board",
                      "**/board/**",
                      "**/src/daemon/app",
                      "**/src/daemon/app/**",
                      "**/src/daemon/platform",
                      "**/src/daemon/platform/**",
                      "**/app",
                      "**/app/**",
                      "**/platform",
                      "**/platform/**",
                      "@effect/platform-bun",
                      "@effect/platform-bun/**",
                      "@effect/sql-sqlite-bun",
                      "@effect/sql-sqlite-bun/**",
                      "@effect/platform",
                      "@effect/platform/**",
                      "@effect/sql",
                      "@effect/sql/**",
                      "@effect/experimental",
                      "@effect/experimental/**"
                    ],
                    "message": "domain modules cannot import app/platform, select native implementations, use Effect v3 packages, or depend on board/**. See tests/import-boundaries.test.ts."
                  }
                ]
              }
            }
          }
        }
      }
    },
    {
      "includes": [
        "contracts/**",
        "scripts/fleet-hook.ts",
        "scripts/fleet-sessionstart.ts",
        "scripts/fleet-watch.ts"
      ],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "patterns": [
                  {
                    "group": [
                      "effect",
                      "effect/**",
                      "@effect",
                      "@effect/**",
                      "**/src/daemon/app",
                      "**/src/daemon/app/**",
                      "**/src/daemon/platform",
                      "**/src/daemon/platform/**",
                      "**/app",
                      "**/app/**",
                      "**/platform",
                      "**/platform/**"
                    ],
                    "message": "contracts and thin hook sources must stay Effect-free and cannot import app/platform composition. The hook seam allowlist is suite-enforced."
                  }
                ]
              }
            }
          }
        }
      }
    },
    {
      "includes": ["src/daemon/app/**", "!src/daemon/app/live-layer.ts"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "patterns": [
                  {
                    "group": [
                      "**/board",
                      "**/board/**",
                      "**/src/daemon/platform",
                      "**/src/daemon/platform/**",
                      "**/platform",
                      "**/platform/**",
                      "@effect/platform-bun",
                      "@effect/platform-bun/**",
                      "@effect/sql-sqlite-bun",
                      "@effect/sql-sqlite-bun/**",
                      "@effect/platform",
                      "@effect/platform/**",
                      "@effect/sql",
                      "@effect/sql/**",
                      "@effect/experimental",
                      "@effect/experimental/**"
                    ],
                    "message": "app workflows cannot select platform implementations; only app/live-layer.ts composes native Layers. Effect v3 packages and board/** remain forbidden."
                  }
                ]
              }
            }
          }
        }
      }
    },
    {
      "includes": ["src/daemon/app/live-layer.ts", "src/daemon/platform/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "patterns": [
                  {
                    "group": [
                      "**/board",
                      "**/board/**",
                      "@effect/platform",
                      "@effect/platform/**",
                      "@effect/sql",
                      "@effect/sql/**",
                      "@effect/experimental",
                      "@effect/experimental/**"
                    ],
                    "message": "Effect composition/platform modules may use the approved v4 cohort but never v3 packages or board/**."
                  }
                ]
              }
            }
          }
        }
      }
    },
    {
      "includes": ["src/daemon/fleetd.ts"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "patterns": [
                  {
                    "group": [
                      "**/board",
                      "**/board/**",
                      "**/src/daemon/platform",
                      "**/src/daemon/platform/**",
                      "**/platform",
                      "**/platform/**",
                      "@effect/platform-bun",
                      "@effect/platform-bun/**",
                      "@effect/sql-sqlite-bun",
                      "@effect/sql-sqlite-bun/**",
                      "@effect/platform",
                      "@effect/platform/**",
                      "@effect/sql",
                      "@effect/sql/**",
                      "@effect/experimental",
                      "@effect/experimental/**"
                    ],
                    "message": "fleetd may import app/live-layer.ts, but native implementation selection stays inside that layer; Effect v3 packages and board/** are forbidden."
                  }
                ]
              }
            }
          }
        }
      }
    }
  ]
}
